# 🎯 RetroBoard - Scrum Retrospective Board

Современное веб-приложение для проведения Scrum-ретроспектив с возможностью добавления идей, мемов и смайлов в реальном времени.

## ✨ Возможности

### Для участников:
- 📝 Добавление текстовых идей
- 🖼️ Добавление мемов через URL или готовые шаблоны
- 😊 Реакции смайлами
- 👍 Голосование за лучшие идеи
- 🔄 Обновление в реальном времени (WebSocket)

### Для ведущего (администратора):
- 🎨 Выбор шаблона ретроспективы:
  - **Классический** (Start, Stop, Continue)
  - **Mad, Sad, Glad**
  - **Good, Bad, Ideas**
  - **KISS** (Keep, Improve, Start, Stop)
  - **Парусник** (Sailboat)
  - **Свободный формат**
- ⚙️ Управление настройками (мемы, смайлы, голосование)
- 📊 Экспорт результатов (JSON, TXT)
- 📋 История всех сессий
- 🗑️ Модерация элементов

## 🚀 Быстрый старт

### Установка

```bash
# Установка зависимостей
npm install

# Запуск в режиме разработки (с авто-перезагрузкой)
npm run dev

# Запуск в продакшене
npm start
```

### 🌐 Выход в интернет (Cloudflare Tunnel)

Для предоставления доступа пользователям из любой страны:

```bash
# 1. Запуск сервера и туннеля (2 окна)
start.bat

# 2. Получить URL туннеля
get-url.bat

# 3. Остановить всё
stop.bat
```

**Важно:** Компьютер должен быть включён, а туннель — работать.

### Использование

1. Откройте браузер: **http://localhost:3000** (локально)
2. Или используйте URL из Cloudflare (для команды)
3. Создайте новую сессию или присоединитесь к существующей
4. Пригласите команду по ID сессии
5. Проведите ретроспективу!

## 📁 Структура проекта

```
Retro/
├── server.js          # Сервер Express + Socket.IO
├── database.js        # База данных SQLite
├── package.json       # Зависимости и скрипты
├── retro.db           # Файл БД (создаётся автоматически)
├── cloudflared.exe    # Cloudflare Tunnel (скачивается автоматически)
├── start.bat          # Запуск сервера + туннеля
├── stop.bat           # Остановка всего
├── get-url.bat        # Получить URL туннеля
├── logs/              # Логи туннеля
└── public/
    ├── index.html     # Главная страница
    ├── styles.css     # Стили
    └── app.js         # Клиентский JavaScript
```

## 🔧 API Endpoints

| Метод | Endpoint | Описание |
|-------|----------|----------|
| POST | `/api/sessions` | Создать сессию |
| GET | `/api/sessions` | Получить все сессии |
| GET | `/api/sessions/:id` | Получить сессию |
| GET | `/api/sessions/:id/items` | Получить элементы сессии |
| POST | `/api/sessions/:id/items` | Добавить элемент |
| PATCH | `/api/sessions/:id/items/:itemId` | Обновить элемент |
| DELETE | `/api/sessions/:id/items/:itemId` | Удалить элемент |
| POST | `/api/sessions/:id/items/:itemId/vote` | Голосовать |
| POST | `/api/sessions/:id/end` | Завершить сессию |

## 🎨 Шаблоны ретроспектив

### Классический
- 🚀 Начать делать
- 🛑 Перестать делать
- ✅ Продолжать делать

### Mad, Sad, Glad
- 😡 Злит
- 😢 Расстраивает
- 😄 Радует

### KISS
- 📌 Keep (Сохранить)
- 🔧 Improve (Улучшить)
- 🚀 Start (Начать)
- 🛑 Stop (Прекратить)

### Парусник
- 💨 Ветер (Что помогает)
- ⚓ Якорь (Что мешает)
- 🪨 Скалы (Риски)
- 🏝️ Остров (Цель)

## 💾 Хранение данных

Все данные сохраняются в SQLite базу данных `retro.db`:
- Сессии ретроспектив
- Элементы (идеи, мемы, смайлы)
- Голоса участников
- Итоги и план действий

## 🔌 WebSocket события

- `item:created` - создан новый элемент
- `item:updated` - обновлён элемент
- `item:deleted` - удалён элемент
- `session:ended` - сессия завершена

## 📊 Отчётность

После завершения сессии можно экспортировать результаты:
- **JSON** - полные данные для импорта
- **TXT** - текстовый отчёт для чтения

История всех сессий доступна на главной странице во вкладке "История".

## 🛠 Технологии

- **Backend:** Node.js, Express, Socket.IO
- **Database:** SQLite (better-sqlite3)
- **Frontend:** Vanilla JS, Bootstrap 5, Material Icons
- **Real-time:** WebSocket через Socket.IO

## 📝 Лицензия

ISC
